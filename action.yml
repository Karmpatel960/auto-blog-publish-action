name: 'Publish Action'
description: 'An action to publish a blog post'
author: 'Karm Patel'
branding:
  icon: 'upload-cloud'
  color: 'green'

inputs:
  hashnode-api-key:
    description: 'API key for Hashnode'
    required: true
  hashnode-blog-id:
    description: 'Blog ID for Hashnode'
    required: true
  repo-token:
    description: 'GitHub repository token'
    required: true
  openai-api-key:
    description: 'API key for OpenAI'
    required: true

outputs:
  publish-output:
    description: 'Output for publish blog'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Run Publish Action
        run: node index.mjs
        env:
          HASHNODE_API_KEY: ${{ inputs.hashnode-api-key }}
          HASHNODE_BLOG_ID: ${{ inputs.hashnode-blog-id }}
          REPO_TOKEN: ${{ inputs.repo-token }}
          OPENAI_API_KEY: ${{ inputs.openai-api-key }}



